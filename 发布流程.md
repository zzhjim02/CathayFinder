# CathayFinder 发布流程

> 每次发新版照着走一遍。**注意：数据库（`data\`，约 27 GB）不进 Git、也不进 GitHub Release。**

## 0. 前提

- 本机 Python 3.10+（打包用 `py -3`）
- 依赖：`py -3 -m pip install PyQt5 pypinyin zhconv`
- 图标：`app.ico` 已在目录里

## 1. 定版本号

改 `fab.py` 顶部的 `VERSION_OVERRIDE`（如 `'1.0'` → `'1.0.1'`）。
（界面标题里没有版本号，所以在这里定。）

## 2. 自检（必须）

```bat
py -3 main.py --selftest
type _selftest.txt
```

打包后**再跑一次 exe 自检**（能抓到「运行时资源没打进去」这类问题）：

```bat
dist\Release\CathayFinder.exe --selftest
```

## 3. 一键打包

```bat
发版.bat
:: 或者
py -3 fab.py
:: 只重算校验值/发行说明（不重新打包）
py -3 fab.py --skip-build
```

产物（都在 `dist\Release\`）：`CathayFinder.exe`、`校验值.txt`、`发行说明_vX.Y.Z.md`。

打完包把新 exe 覆盖目录里的「`CathayFinder 综合性图书检索引擎 1.0 标准版.exe`」，
给用户的还是那一个文件，数据不用动。

## 4. 上传

1. **百度网盘**（给普通用户：exe + `data\`，或打成 zip）→ 分享链接（密码 2026）填进 README
2. **GitHub Releases**：

```bat
gh release create vX.Y.Z "dist\Release\CathayFinder.exe" ^
   --title "CathayFinder vX.Y.Z" --notes-file "dist\Release\发行说明_vX.Y.Z.md"
```

3. 回填 README 的下载表与更新日志

## 5. 记住几条坑

- **`--name` 绝不能和源码包同名**：源码包是 `cathayfinder\`，Windows 大小写不敏感，
  所以 `--name` **别**写成 `cathayfinder`，`--distpath` 也别指到源码包那一层 ——
  曾经就是这么把源码包整个删掉的（后来从打包缓存的 PYZ 里恢复出来的）。
- **运行时资源要显式带上**：`--collect-all pypinyin --collect-all zhconv`，否则 exe 里
  拼音排序 / 繁简转换会缺数据。
- **`data\` 不进包**：体积太大；但**发布给用户时必须让 exe 与 `data\` 同层**。
- `.bat` 里不要写中文（cmd 按 GBK 解析会乱），本目录的 `发版.bat` 是纯 ASCII。
